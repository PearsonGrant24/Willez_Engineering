deployment:
  tasks:
    - export DEPLOYPATH=/home/willeze1/public2_html/
    - /bin/rm -rf $DEPLOYPATH*
    - /usr/local/bin/npm install
    - /usr/local/bin/npm run build
    - /bin/cp -R build/* $DEPLOYPATH
